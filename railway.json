{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "cd backend && python manage.py migrate && daphne -b 0.0.0.0 -p $PORT config.asgi:application",
    "restartPolicyType": "ON_FAILURE"
  }
}
